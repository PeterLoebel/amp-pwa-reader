declare class FeedEntry {
    title: string;
    description: string;
    link: string;
    image: string;
    content: Array<string>;
    constructor(title: string, description: string, link: string, image: string, content: Array<string>);
}
